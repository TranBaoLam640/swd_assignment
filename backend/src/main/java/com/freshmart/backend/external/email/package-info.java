/**
 * Integration with external email providers (e.g. SMTP, SendGrid) used across
 * the whole application (order confirmations, OTP, password reset, notifications).
 */
package com.freshmart.backend.external.email;
