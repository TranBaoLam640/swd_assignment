/**
 * Repositories for the Cart Management module (CartItemRepository). No
 * separate CartRepository/Cart entity — per the SDS class diagram, a
 * customer's cart is simply the set of CartItem rows for their userId.
 */
package com.freshmart.backend.data_access.repository.cart_management_module;
