package com.freshmart.backend.enums.authentication_and_user_account;

/** Matches the RoleType enum in the authentication_and_user_account class diagram. */
public enum RoleType {
    CUSTOMER,
    MANAGER,
    SHIPPER,
    ADMIN
}
