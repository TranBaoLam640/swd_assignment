/**
 * Address Management feature (maps to backend module
 * `address_management_module`): a customer's saved shipping addresses —
 * add, edit, delete, and set default. The first address a customer saves
 * is always forced default by the backend.
 */
export { AddressListPage } from "./pages";
