/**
 * Inventory Management feature (maps to backend module
 * `inventory_management_module`): stock increase/decrease/set. No
 * standalone page yet — stock controls are embedded directly inside the
 * Product Management manager screens (see ManagerProductListPage).
 */
