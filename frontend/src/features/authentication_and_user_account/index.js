/**
 * Authentication & User Account feature (maps to backend module
 * `authentication_and_user_account`): login, register, forgot/reset
 * password, view/update profile.
 */
export { LoginPage, RegisterPage } from "./pages";
